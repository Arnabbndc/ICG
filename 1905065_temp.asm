	SUB SP, 2
	SUB SP, 20
	SUB SP, 2
	PUSH 1
	PUSH 5
	POP BX
	POP AX
	ADD AX, BX
	PUSH AX
	PUSH 7
	POP BX
	POP AX
	ADD AX, BX
	PUSH AX
	POP AX
	MOV a, AX
	PUSH AX
	POP AX
	PUSH a
	PUSH 2
	POP BX
	POP AX
	ADD AX, BX
	PUSH AX
	POP AX
	MOV [BP-2], AX
	PUSH AX
	POP AX
	PUSH 4
	POP AX
	SHL AX, 1
	LEA SI, [BP-4]
	SUB SI, AX
	PUSH SI

	PUSH 10
	POP AX
	POP SI
	MOV [SI], AX
	PUSH AX
	POP AX
	MOV AX, a
	CALL print_output
	MOV AX, [BP-2]
	CALL print_output
