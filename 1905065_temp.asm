	SUB SP, 2
	SUB SP, 20
	SUB SP, 2
	PUSH 1
	POP AX
	MOV a, AX
	PUSH AX
	POP AX
	PUSH 3
	POP AX
	MOV [BP-2], AX
	PUSH AX
	POP AX
	PUSH 4
	POP AX
	SHL AX, 1
	LEA BX, [BP-4]
	SUB BX, AX
	PUSH BX

	PUSH 10
	POP AX
	POP BX
	MOV [BX], AX
	PUSH AX
	POP AX
	MOV AX, a
	CALL print_output
	MOV AX, [BP-2]
	CALL print_output
